[gd_resource type="Shader" format=2]

[resource]
code = "shader_type spatial;
render_mode unshaded, world_vertex_coords;

void fragment() {
	
	vec3 red = vec3(0.2, 0, 0);
	vec3 white = vec3(1.0);
	
	vec3 mix1 = mix(red, white, UV.x);
    mix1 = pow(mix1.rgb, vec3(2.2));	
	ALBEDO = mix1;
}"
